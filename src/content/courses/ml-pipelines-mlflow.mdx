---
title: "ML Pipelines & MLflow"
track: "mlops"
summary: "Build a real ML pipeline with MLflow tracking, model registry, and PyFunc wrappers — the exact pattern behind production model serving at scale."
outcomes:
  - "Track experiments, parameters, and metrics with MLflow the way production teams actually use it."
  - "Build a reusable MLflow PyFunc wrapper — the same pattern used to migrate 6+ models to serverless serving in production."
  - "Understand model/artifact versioning and lineage tracking end to end, from training run to live model."
modules:
  - title: "MLflow Fundamentals"
    lessons: ["Experiment tracking", "Model registry", "Unity Catalog volumes for versioning"]
  - title: "The PyFunc Wrapper Pattern"
    lessons: ["Building a reusable serving wrapper", "Scale-to-zero configuration", "Lineage: live model back to training run"]
prerequisites: ["Python for Engineers"]
status: "coming-soon"
order: 4
---

The free flagship lesson planned for this track: **"Deploy your first model with MLflow on
Databricks — serverless, scale-to-zero."** Everything else in this course builds on that same
pattern, scaled to a full pipeline.
